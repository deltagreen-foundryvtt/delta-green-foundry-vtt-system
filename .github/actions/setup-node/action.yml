name: "Setup Node Project"
description: "Sets up a Node.js project"

runs:
  using: "composite"
  steps:
    - name: Use Node.js
      uses: actions/setup-node@v6
      with:
        node-version: "24"
        cache: npm

    - name: Install dependencies
      shell: bash
      run: npm ci
